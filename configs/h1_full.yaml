experiment_name: h1-sft-full-pilot

data:
  train_path: data/generated/h1/seed_30072026/train.jsonl
  test_path: data/generated/h1/seed_30072026/test.jsonl

model:
  name_or_path: .models/Qwen3-0.6B
  revision: c1899de289a04d12100db370d81485cdf75e47ca
  local_files_only: true

# Single <1B full-fine-tuning pilot. These are concrete values from the
# recommended ranges, not a sweep or checkpoint-selection protocol.
training:
  # Effective batch = 16 micro-batch x 4 accumulation steps = 64 sequences.
  batch_size: 16
  gradient_accumulation_steps: 4
  learning_rate: 5.0e-5
  lr_scheduler: cosine
  final_learning_rate_ratio: 0.1
  warmup_ratio: 0.05
  warmup_min_steps: 20
  epochs: 3
  smoke_steps: null
  max_sequence_length: 1024
  seed: 42
  precision: bf16
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_epsilon: 1.0e-8
  weight_decay: 0.0
  max_grad_norm: 1.0

evaluation:
  # Runs at epoch 0 before the first optimizer step and after every epoch.
  batch_size: 64

runtime:
  device: auto
  debug: false
  prediction_log_limit: null
  output_dir: artifacts/h1_full_pilot
  save_model: true

tracking:
  enabled: true
  project_name: do-models-only-memorize
  project_id: null
  task_name: null
  # Scalars go to ClearML; MLC exports all local artifacts from /outputs/run.
  output_uri: false
  log_interval_steps: 1
  tags: [sft, h1, full, pilot, qwen3-0.6b]
